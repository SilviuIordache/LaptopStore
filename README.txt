1. Make sure you have Node.js installed, in the terminal: node -v
2. If not, install it using: 
	npm install node
2. Install nodemon: 
	npm install nodemon -g
3. Run the following command in the Visual Code terminal: 
	nodemon